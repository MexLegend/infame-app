export type Category = {
    billboard: string;
    billboardId: string;
    createdAt?: Date;
    id?: string;
    name: string;
    products: string[];
    store: string;
    storeId: string;
    updatedAt?: Date;
}